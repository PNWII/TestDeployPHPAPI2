import React from "react";
declare const NavigateButton: ({ className, type, disabled, onClick, children, ...props }: React.ComponentProps<"button">) => React.JSX.Element;
export default NavigateButton;
